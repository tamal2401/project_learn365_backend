spring:
  application:
    name: course-portfolio-services
---
spring:
  profiles: test
  cloud:
    stream:
      bindings:
        usercourseSubscription:
          destination: learn365_course_subscription_exchange
          group: learn365_course_management_group
          contentType: application/json
          binder: rabbit
        videoTestStatusListener:
          destination: learn365_course_video_test_status_exchange
          group: learn365_course_management_group
          contentType: application/json
          binder: rabbit
      default-binder: rabbit
      rabbit:
        default:
          type: rabbit
          bindingRoutingKey: learn365_course_management_group
          exchangeType: topic
        binder:
          brokers: localhost
          autoCreateTopics: true
          autoAddPartitions: true
          defaultBrokerPort: 5672
          username: guest
          password: guest

  datasource:
    driver: com.mysql.jdbc.Driver
    url: jdbc:mysql://database-1.ckbg4rgzbz67.ap-south-1.rds.amazonaws.com:3306/course_offering
    username: learn365
    password: 0nlineeducation
    hikari:
      maximum-pool-size: 3
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8999/eureka

security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9000/oauth/check_token
    client:
      client-id: learn365onlineeducation
      client-secret: le1rnDigit1lCourse

learn365:
  security:
    ignore: /v2/api-docs,/configuration/ui,/swagger-resources/**,/configuration/security,/swagger-ui.html,/webjars/**,/v1/user/service/**,/v1/grade/service/**,/v1/chapter/service/**,/v1/grade/getAllGrade,/v1/trial/service/**

  trial:
    period:
      validity: 15

  logging:
    directory: /var/log/lear365-log
    logstash:
      host: localhost:5043

---

spring:
  profiles: cloud
  cloud:
    stream:
      bindings:
        usercourseSubscription:
          destination: learn365_course_subscription_exchange
          group: learn365_course_management_group
          contentType: application/json
          binder: rabbit
        videoTestStatusListener:
          destination: learn365_course_video_test_status_exchange
          group: learn365_course_management_group
          contentType: application/json
          binder: rabbit
      default-binder: rabbit
      rabbit:
        default:
          type: rabbit
          bindingRoutingKey: learn365_course_management_group
          exchangeType: topic
        binder:
          brokers: localhost
          autoCreateTopics: true
          autoAddPartitions: true
          defaultBrokerPort: 5672
          username: guest
          password: guest

  datasource:
    driver: com.mysql.jdbc.Driver
    url: jdbc:mysql://database-1.ckbg4rgzbz67.ap-south-1.rds.amazonaws.com:3306/course_offering
    username: learn365
    password: 0nlineeducation
    hikari:
      maximum-pool-size: 3
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8999/eureka

security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9000/oauth/check_token
    client:
      client-id: learn365onlineeducation
      client-secret: le1rnDigit1lCourse

learn365:
  security:
    ignore: /v2/api-docs,/configuration/ui,/swagger-resources/**,/configuration/security,/swagger-ui.html,/webjars/**,/v1/user/service/**,/v1/grade/service/**,/v1/chapter/service/**,/v1/grade/getAllGrade,/v1/trial/service/**

  trial:
    period:
      validity: 15

  logging:
    directory:
    logstash:
      host: